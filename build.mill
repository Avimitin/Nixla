package build
import mill.*, scalalib.*

object nixla extends ScalaModule {
  def scalaVersion = "3.7.4"
  def mainClass    = Some("nixla.demo")

  object test extends ScalaTests, TestModule.Munit {
    def mvnDeps = Seq(mvn"org.scalameta::munit:1.1.1")
  }
}
