package build
import mill._, scalalib._

object scalanix extends ScalaModule {
  def scalaVersion = "3.3.6"
  def mainClass    = Some("scalanix.demo")

  object test extends ScalaTests with TestModule.Munit {
    def ivyDeps = Agg(ivy"org.scalameta::munit:1.1.1")
  }
}
